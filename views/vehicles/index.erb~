<p><strong><%= "Hi, #{@user}" %></strong></p>
<a href="/vehicles/new" class="btn">Add a car</a>
<hr />
<ul id="vehicle_list">
<% @vehicles.each do |v| %>
  <% if v.sale_date == "" %>
    <a href="vehicles/show/<%= v.id %>" class="for_sale"><li><%= "#{v.year} #{v.make} #{v.model}" %> - Days in stock: <%= days_in_stock(v.id) %></li></a>
  <% else %>
    <a href="vehicles/show/<%= v.id %>" class="sold"><li><%= "#{v.year} #{v.make} #{v.model}" %> - Days in stock: <%= days_in_stock(v.id) %></li></a>
  <% end %>  
<% end %>
</ul>
